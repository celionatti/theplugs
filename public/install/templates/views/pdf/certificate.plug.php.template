<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Certificate - {{ $participant_name ?? 'Recipient Name' }}</title>
    <style>
        @page {
            margin: 0;
            size: a4 landscape;
        }

        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 0;
            background-color: #fff;
            color: #2c3e50;
        }

        .outer-border {
            border: 20px solid #f1c40f;
            height: 520px;
            /* Approximate for landscape A4 */
            padding: 20px;
            margin: 20px;
        }

        .inner-border {
            border: 5px solid #2c3e50;
            height: 470px;
            padding: 40px;
            text-align: center;
            position: relative;
        }

        .certificate-header {
            font-size: 50px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 5px;
            margin-bottom: 20px;
            color: #2c3e50;
        }

        .certificate-sub-header {
            font-size: 20px;
            font-style: italic;
            margin-bottom: 30px;
        }

        .participant-name {
            font-size: 45px;
            font-family: 'Times New Roman', serif;
            font-weight: bold;
            border-bottom: 2px solid #2c3e50;
            display: inline-block;
            margin: 20px 0;
            padding: 0 50px;
            color: #2980b9;
        }

        .certificate-content {
            font-size: 18px;
            line-height: 1.6;
            width: 80%;
            margin: 0 auto;
        }

        .certificate-footer {
            margin-top: 60px;
            display: table;
            width: 100%;
        }

        .footer-col {
            display: table-cell;
            width: 33%;
            vertical-align: bottom;
        }

        .signature-line {
            border-top: 1px solid #2c3e50;
            width: 80%;
            margin: 0 auto 10px;
        }

        .signature-label {
            font-size: 14px;
            font-weight: bold;
        }

        .seal {
            width: 100px;
            height: 100px;
            background-color: #f1c40f;
            border-radius: 50%;
            display: inline-block;
            position: relative;
            box-shadow: 0 0 0 5px #f1c40f, 0 0 0 10px rgba(241, 196, 15, 0.5);
        }

        .seal-star {
            color: white;
            font-size: 40px;
            line-height: 100px;
        }
    </style>
</head>

<body>
    <div class="outer-border">
        <div class="inner-border">
            <div class="certificate-header">Certificate</div>
            <div class="certificate-sub-header">OF ACHIEVEMENT</div>

            <div style="font-size: 18px;">This is to certify that</div>

            <div class="participant-name">{{ $participant_name ?? 'Celio Natti' }}</div>

            <div class="certificate-content">
                {{ $certificate_body ?? 'has successfully completed the Professional PHP Web Development Course with
                outstanding performance and dedication to mastering modern framework architectures.' }}
            </div>

            <div class="certificate-footer">
                <div class="footer-col">
                    <div class="signature-line"></div>
                    <div class="signature-label">INSTRUCTOR</div>
                </div>
                <div class="footer-col">
                    <div class="seal">
                        <div class="seal-star">★</div>
                    </div>
                </div>
                <div class="footer-col">
                    <div class="signature-line"></div>
                    <div class="signature-label">DIRECTOR</div>
                </div>
            </div>

            <div style="margin-top: 30px; font-size: 12px; color: #7f8c8d;">
                Issued on: {{ $date ?? date('d M Y') }} &bull; Verify Identifier: {{ $verify_id ??
                'PLG-CERT-'.rand(1000,9999) }}
            </div>
        </div>
    </div>
</body>

</html>